<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.study.mapper.EmpMapper">


    <select id="getEmpById" resultType="emp">
        SELECT id, emp_name, salary, dept_id
        FROM emp
        WHERE  id = #{id}
    </select>

    <insert id="" parameterType="" useGeneratedKeys="" keyProperty=""
     databaseId="" flushCache="" keyColumn="" lang="" parameterMap=""
    statementType="" timeout=""></insert>
</mapper>